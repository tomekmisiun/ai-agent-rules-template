name: Validate AI rules

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  validate-ai-rules:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Static AI workflow validation
        run: make validate
